---
demos:
  local-demo-test-env:
    description: Test demo. Provided with the STACKABLE_DEMO_FILES env var in the .env file
    stackableStack: test
    labels:
      - test
      - local
    manifests:
      - plainYaml: lol
